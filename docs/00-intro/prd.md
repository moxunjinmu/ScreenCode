# ScreenCode 产品需求文档 (PRD)

> 版本：v2.6 - Neo Utility 双主题仿真完成
> 最后更新：2026-08-03
> 状态：已实现功能整理

---

## 一、产品概述

### 1.1 产品定位

ScreenCode 是一个 Electron 桌面应用，用于**隔离网络环境下的屏幕捕获和代码提取**。

**核心场景**：外包开发者在内网机器上编写代码，通过采集卡将内网屏幕画面传输到外网个人电脑，ScreenCode 捕获画面后使用多供应商 AI API（智谱 GLM-5、Claude Sonnet 等）进行代码识别和提取，支持多轮 AI 对话和会话管理。

### 1.2 目标用户

在物理隔离网络环境下工作的外包开发者，需要将内网屏幕上的代码快速还原为可编辑文本。

### 1.3 核心价值主张

- **快速提取**：热键截图 → AI 识别 → 代码输出，端到端延迟 < 20s
- **多供应商支持**：4 个 AI 供应商自动路由（Anthropic、智谱标准/Anthropic 兼容、OpenRouter）
- **心流保护**：全局热键操作，Toast 通知，系统托盘常驻
- **会话管理**：多会话并行，标题自动生成，内存存储

---

## 二、已实现功能清单

### 2.1 MVP 核心功能（Phase 1 ✅）

| 功能 | 状态 | 说明 |
|------|------|------|
| 全局热键截图 | ✅ | Ctrl+Shift+S 捕获当前帧 |
| 环形缓冲区 | ✅ | 最多存储 8 帧，RingBuffer 实现 |
| 图像压缩 | ✅ | Sharp 1080p→768px, JPEG Q=85 |
| 代码提取 | ✅ | Ctrl+Shift+E，结构化 Prompt → JSON 输出 |
| 系统托盘 | ✅ | 常驻托盘，右键菜单 |
| Toast 通知 | ✅ | 1.5s 自动消失 |
| 代码展示 | ✅ | 只读展示 + 一键复制 |

### 2.2 Phase 2 已实现功能

| 功能 | 状态 | 完成时间 |
|------|------|----------|
| 多轮 AI 对话 | ✅ | 2026-02-27 |
| 会话管理 | ✅ | 2026-02-28 (新建/切换/删除) |
| 会话标题自动生成 | ✅ | 首条用户消息前 20 字符 |
| 全屏预览模式 | ✅ | 2026-02-27 |
| 区域截图 | ✅ | 2026-02-28 (RegionCaptureOverlay) |
| 帧队列交互 | ✅ | 2026-03-02 (选中/删除) |
| 聊天面板拖拽 | ✅ | 2026-03-02 (宽度可调，<200px 关闭) |
| 配置实时推送 | ✅ | 2026-02-28 (CONFIG_CHANGED) |
| 多供应商配置 | ✅ | 2026-02-27 (4 供应商独立配置) |

### 2.3 未实现功能（后续计划）

| 功能 | 优先级 | 备注 |
|------|--------|------|
| 真实视频采集设备枚举和捕获 | P0 | 当前使用硬编码数据 |
| 像素级帧差分算法 | P0 | 当前返回模拟值 0.3 |
| 系统托盘状态图标变体 | P1 | 绿=连接/红=断开/黄=处理中 |
| 流式输出 (SSE) | P1 | 聊天响应流式传输 |
| 本地 VLM 路由（合规模式） | P2 | GLM-4V / Qwen-VL via Ollama |
| 审计日志 | P2 | better-sqlite3 append-only 表 |
| 本地脱敏层 | P2 | NER + 正则掩码 |

---

## 三、技术架构

### 3.1 技术栈

| 层次 | 技术选型 | 用途 |
|------|----------|------|
| 运行时 | Electron ^28 | 桌面应用框架 |
| 构建 | Electron Forge + Vite | 开发与打包 |
| 前端 | React 18 + TypeScript | UI 框架 |
| 状态管理 | Zustand | 5 个独立 Store |
| 样式 | TailwindCSS | 原子化 CSS |
| 图像处理 | Sharp | 高性能压缩 |
| AI SDK | @anthropic-ai/sdk + openai | 双 SDK 自动路由 |
| 配置存储 | electron-store | 持久化配置 |

### 3.2 进程模型

```
┌─────────────────────────────────────────┐
│         Electron Application            │
├─────────────────────────────────────────┤
│  Main Process (src/main/)               │
│  - Capture: 视频采集 (TODO: 真实实现)    │
│  - Processor: RingBuffer/FrameDiff/Sharp│
│  - AI: 服务路由 + Claude/OpenAI 封装      │
│  - Config: electron-store + IPC 推送     │
│  - Tray: 系统托盘                        │
├─────────────────── IPC ─────────────────┤
│  Renderer Process (src/renderer/)       │
│  - React 18 + Zustand + TailwindCSS     │
│  - Store: capture/frame/app/chat/ui     │
│  - Components: Preview/ChatPanel/...    │
├─────────────────────────────────────────┤
│  Preload (src/preload/)                 │
│  - contextBridge 安全隔离               │
└─────────────────────────────────────────┘
```

### 3.3 AI 服务路由

`isOpenAICompatible(baseUrl)` 根据 baseUrl 自动选择 SDK：

| Base URL 匹配 | 使用 SDK | 供应商 |
|--------------|----------|--------|
| `/api/anthropic` | Anthropic SDK | zhipu-anthropic |
| `bigmodel.cn` | OpenAI SDK | zhipu (标准端点) |
| `openrouter.ai` | OpenAI SDK | openrouter |
| 其他 | Anthropic SDK | anthropic (默认) |

### 3.4 多供应商配置

支持 4 个 API 供应商，独立配置：

| ID | 名称 | Base URL | SDK |
|----|------|----------|-----|
| `anthropic` | Anthropic 官方 | api.anthropic.com | Anthropic |
| `zhipu` | 智谱 AI 标准 | open.bigmodel.cn/api/paas/v4 | OpenAI |
| `zhipu-anthropic` | 智谱 Anthropic 兼容 | open.bigmodel.cn/api/anthropic | Anthropic |
| `openrouter` | OpenRouter | openrouter.ai/api/v1 | OpenAI |

---

## 四、性能指标

### 4.1 当前性能目标

| 指标 | 目标值 | 状态 |
|------|--------|------|
| 热键到 Toast 延迟 | < 200ms | ✅ 已达成 |
| 代码提取超时 | 20s | ✅ 已实现 |
| AI 聊天响应 | < 8s | ✅ 已实现 |
| Ring Buffer 容量 | 8 帧 | ✅ 已实现 |
| 图像压缩宽度 | 768px (JPEG Q=85) | ✅ 已实现 |
| Toast 持续时间 | 1.5s | ✅ 已实现 |

### 4.2 待实现性能优化

| 指标 | 目标值 | 依赖 |
|------|--------|------|
| 帧差分算法精度 | 像素级对比 | Sharp 采样实现 |
| 托盘状态图标 | 3 状态变体 | 图标设计资源 |
| 流式输出延迟 | < 1s 首字 | SSE 实现 |

---

## 五、用户体验设计

### 5.1 布局结构

```
┌─────────────────────────────────────────────┐
│  [实时预览]                                  │
│                                             │
├─────────────────────────────────────────────┤
│  [缩略图队列 ×8]                             │
├─────────────────────────────────────────────┤
│  [代码展示]                                  │
│                                             │
├──────────────┬──────────────────────────────┤
│  主内容区     │  ChatPanel (可拖拽/可关闭)    │
│              │                              │
└──────────────┴──────────────────────────────┘
```

### 5.2 全局热键

| 热键 | 动作 |
|------|------|
| Ctrl+Shift+S | 捕获当前帧入队 |
| Ctrl+Shift+E | 提取代码 |
| Ctrl+Shift+M | 打开主窗口 |

### 5.3 交互增强

- **帧队列**：点击选中/取消选中，hover 显示删除按钮
- **聊天面板**：拖拽宽度调节，<200px 自动关闭，300ms 过渡动画
- **全屏预览**：独立布局模式，视频全屏查看

---

## 六、开发路线图

### Phase 1 ✅ (已完成)

- [x] 基础截图入队链路
- [x] AI 代码提取
- [x] 多供应商配置
- [x] 会话管理
- [x] 全屏预览/区域截图

### Phase 2 🚧 (进行中)

- [ ] 真实视频采集设备枚举和捕获
- [ ] 像素级帧差分算法
- [ ] 系统托盘状态图标

### Graphite Capture 视觉工作台原型 ✅（2026-08-03）

- [x] 生成宽屏双栏工作台仿真页：左侧采集与帧队列，右侧代码结果与 AI 对话
- [x] 生成 800×600 紧凑模式：采集 / 代码 / AI 标签页与可折叠帧队列
- [x] 原型覆盖帧选择、模拟截图、代码提取、AI 对话、显示设置和应用设置交互
- [x] 输出宽屏与紧凑两档设计稿截图，完成离线打开与响应式验收
- [x] 原型仅用于视觉和交互决策，不替代现有 Electron 业务实现
- [x] 结合网络现代生产力工具趋势，生成 Neo Utility 暖黑 / 酸性青柠配色候选稿，待产品确认
- [x] 生成与暗色稿同构的 Neo Utility 暖纸白亮色主题候选稿，待产品确认
- [x] 移除亮色候选稿中的产品说明、操作教学和重复快捷键等常驻描述文案

### Neo Utility 双主题仿真与真实项目后续计划 🔄（2026-08-03）

- [x] 产品确认采用极简双栏布局、暖纸白亮色与暖灰近黑暗色方向
- [x] 仿真页使用同一套语义 Token 实现亮色 / 暗色即时切换
- [x] 仿真页记忆手动主题选择，未选择时跟随系统主题
- [x] 移除产品说明、操作教学和重复快捷键等常驻描述文案
- [x] 从可运行仿真页输出亮色 / 暗色两张一致性设计稿
- [ ] 后续真实 Electron 项目接入全局主题状态、持久化与系统主题监听

验收标准：主题切换无需刷新；功能状态与布局不因主题变化而改变；两套主题正文对比度满足
WCAG AA；酸性青柠仅用于主操作与选中态，柔紫仅用于 AI 状态；800×600 紧凑模式仍可完成
采集、选帧、提取与 AI 对话主流程。

### Phase 3 📋 (计划中)

- [ ] 流式输出 (SSE)
- [ ] 本地 VLM 路由（合规模式）
- [ ] 审计日志
- [ ] 本地脱敏层
- [ ] 自定义热键配置
- [ ] 多语言支持 (i18n)

---

## 七、风险与约束

### 7.1 技术风险

1. **视频采集未实现**：当前使用硬编码数据，需实现真实的 desktopCapturer API
2. **帧差分算法简化**：当前返回模拟值，需实现像素级对比算法

### 7.2 合规注意事项

> ⚠️ **重要提示**：本产品涉及屏幕内容采集和云端 AI API 调用，在金融、政府等敏感行业使用时需注意：
>
> 1. **数据出境风险**：调用云端 API 可能将屏幕内容传输至境外服务器
> 2. **敏感信息保护**：请勿截取含个人隐私、商业机密、密钥等敏感信息的画面
> 3. **机构政策合规**：使用前请确认符合所在机构的信息安全政策

### 7.3 使用约束

- Ring Buffer 最大容量：8 帧
- 帧差分阈值：5%（可在配置中调整）
- 聊天图片上限：4 张/消息
- AI API 超时：25 秒

---

## 八、附录

### 8.1 相关文档

- [项目概览](overview.md)
- [路线图](roadmap.md)
- [架构设计](../01-architecture/overview.md)
- [变更日志](../08-history/changelog.md)

### 8.2 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.0 | 2026-03-31 | 基于实际实现重写 |
| v1.0 | 2026-02-27 | 初版 PRD 重构方案 |
