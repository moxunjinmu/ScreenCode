ScreenCode PRD 重构：顶尖版本 v1.0
核心结论：初版 PRD 是一个"功能堆叠"的产品草稿，而非一个可落地的工程方案。其最致命的问题不是 UX 笨重，而是将一个合规性地雷包装成了产品卖点。以下是系统性重构。

一、核心痛点与合规重塑
硬件采集+AI 链路的三层合规地雷
初版方案在合规层面存在三个结构性致命伤，任何一个被甲方法务部门识别，产品即刻出局：

地雷一：数据出境

将内网屏幕画面传至外网个人电脑，本质上是一条未授权的数据渗漏通道。根据《数据安全法》第31条及《网络安全法》，银行类金融机构属于关键信息基础设施，数据出境须经国家网信办安全评估。外包开发者私自架设采集卡链路，法律性质等同于未授权的数据拷贝行为。
​

地雷二：第三方云 API 调用

调用 Claude/GLM 云端 API = 将含有潜在内网代码的截图上传至境外/第三方云服务器。这直接违反银保监会《商业银行数据治理指引》中关于"核心数据不得存储于境外"的硬性规定，与等保 2.0/3.0 的数据隔离要求冲突。
​

地雷三：无审计痕迹

初版截图队列为明文本地存储，无操作日志。在合规审计场景下，无法证明"什么数据被采集了、被发给了谁"。这不仅是监管风险，也是外包团队对甲方的责任豁免漏洞。

产品级合规重塑设计
产品设计必须将合规能力作为一等公民内嵌，而非事后贴标签：

本地脱敏层（Pre-flight Sanitizer）：截图进入 VLM 调用前，本地运行正则+NER扫描，自动掩码手机号、身份证、银行账号、密钥串、IP地址，脱敏结果可视化供用户确认后再发送

双轨模型路由：用户可配置"合规模式"，高敏感内容强制路由至本地部署 VLM（GLM-4V / Qwen-VL via Ollama），禁止调用任何云端 API
​

不可篡改审计日志：所有操作（截图触发、API调用、文件写入）写入 better-sqlite3 的 append-only 表，记录时间戳、操作类型、脱敏摘要、调用耗时，日志文件不对用户暴露删除入口

首次启动合规声明：强制用户勾选"本人已获所在机构书面授权，方可使用本工具采集屏幕内容"，记录确认时间戳存入审计日志

数据分级标签：项目级别可设置"公开 / 内部 / 机密"，机密级别自动锁定本地模型，且禁用 Obsidian Cloud Sync

二、产品形态与交互重构（UX）
初版设计的根本性错误
初版的"左侧预览+底部缩略图+右侧对话"布局，要求用户在内网机器和外网机器之间反复切换注意力，同时在 ScreenCode 的主窗口中操作截图队列。这完全违背了目标用户的使用场景——他们 90% 的时间是在内网机器上写代码，外网机器只是一个辅助工具。

设计原则重写：软件在 99% 的时间里必须是隐形的，仅在用户主动触发时瞬间响应，响应结束后立刻退隐。

「幽灵模式」三层悬浮窗交互设计
text
Layer 0 ──────── 系统托盘常驻（永远在线，零 UI 占用）
                  采集卡信号状态 → 托盘图标颜色（绿=有信号 / 红=无信号）

Layer 1 ──────── 全局热键（手不离键盘，<100ms 响应）
  Ctrl+Shift+S   → 捕获当前帧入队 + 右下角 Toast 1.5s 自动消失
  Ctrl+Shift+A   → 唤起 Mini 悬浮窗（附带最近 3 帧缩略图）
  Ctrl+Shift+E   → 对队列所有帧执行代码提取，结果弹至 Mini 窗

Layer 2 ──────── Mini 悬浮窗（心流态，按需浮现）
  宽 320px，半透明可拖拽，始终置顶
  [缩略图×3] + [单行输入框] + [▶发送]
  AI 回答以气泡形式展示，按 Esc 收起

Layer 3 ──────── 主界面（深度工作态，按需打开）
  托盘右键"打开主界面" 或 Ctrl+Shift+M
  完整代码查看 / 多轮对话历史 / Obsidian 归档管理
关键 UX 数字目标：

操作	目标延迟
热键触发 → Toast 消失（截图入队）	< 200ms
代码提取（8帧 → 完整代码）	< 20s（P90）
Mini 悬浮窗 AI 问答	< 8s（P90）
悬浮窗透明度	用户可调 10%–90%
三、核心技术架构演进
当前"多图拼接"方案的四大技术瓶颈
瓶颈一：行号错乱

多张截图独立送入 VLM，模型对每张图单独识别，相邻截图间没有语义连接。函数体被截断于不同图片边缘，跨图的变量名因字体渲染差异出现识别不一致（如 l 与 1、O 与 0），行号重置或跳跃。
​

瓶颈二：上下文丢失

每次 API 调用是独立的——模型不知道"第二张图是第一张图向下滚动的续集"，无法推断跨图的代码结构层次（class 嵌套、缩进关系、装饰器归属）。

瓶颈三：重复与遗漏

手动排序依赖用户记忆，极易漏截或重叠截取同一段代码，合并后需要人工去重，完全抵消了 AI 辅助的价值。

瓶颈四：分辨率-成本失控

采集卡输出通常为 1080p，直接送 Claude Vision API 的 token 消耗约为 768px 图的 2.8 倍。8张 1080p 图单次调用成本约为 $0.15，规模化后不可接受。
​

顶尖方案：多帧缓冲区 + 锚点拼接 + 结构化提示工程
核心架构流程：

text
[采集卡 HDMI/USB]
      ↓
[getUserMedia 视频流 @30fps]
      ↓
[Ring Buffer 环形缓冲区 N=8帧]
      ↓ 帧差分引擎（像素 diff 阈值分类）
      │  diff < 5%   → 丢弃（静态重复帧）
      │  diff 5-60%  → 保留，标记 "continuation"（滚动帧）
      │  diff > 60%  → 保留，标记 "new_scene"（场景切换）
      ↓
[Sharp 图像预处理：1080p → 768px宽，JPEG Q=85]
      ↓
[本地脱敏层：NER + 正则掩码]
      ↓
[敏感度路由器]
      ├─ 合规模式 → 本地 VLM（GLM-4V / Qwen-VL via Ollama）
      └─ 标准模式 → 云端 VLM（Claude 3.5 Sonnet API）
              ↓
[结构化 Prompt 工程]
              ↓
[JSON 代码输出 + 置信度评分]
      ↓
[Mini 悬浮窗展示 + 主界面多轮对话]
      ↓
[Obsidian Markdown 写入 + SQLite 审计日志]
结构化 Prompt 核心设计（替代朴素的"多图合并"）：

发送给 VLM 的 Prompt 中须包含帧元数据注释，引导模型理解时序关系：

text
你将收到 {N} 张按时序排列的代码截图，它们来自同一文件的连续滚动操作。
相邻截图之间存在重叠行，请去重并输出完整连贯代码。
[帧1/8 | 类型:new_scene] <image>
[帧2/8 | 类型:continuation | 与上帧重叠约30%] <image>
...
输出格式：{"language":"...", "code":"...", "confidence":0.0-1.0}
精准技术选型建议
层次	初版选型	重构建议	核心理由
构建工具	未明确	Electron Forge + Vite	Vite 热更新比 CRA 快 10 倍，打包体积减少 40%
状态管理	未提及	Zustand	轻量，天然适配 Electron IPC 异步状态，替代 Redux
视频采集	getUserMedia	getUserMedia + desktopCapturer 双通道	外接采集卡枚举用 getUserMedia；屏幕录制 fallback 用 desktopCapturer 
​
图像处理	无	Sharp（主进程 Native 模块）	高性能 1080p→768px 压缩，token 成本降低 65%
VLM 云端	GLM-4V-Flash 或 Claude	Claude 3.5 Sonnet（首选）	代码文本识别准确率及不规则图片转录能力显著优于同级模型 
​
VLM 本地	无	GLM-4V / Qwen2-VL via Ollama	合规模式必选，支持本地部署零数据外传 
​
本地存储	Obsidian 直写 fs	Obsidian fs + better-sqlite3	fs 写 Markdown；SQLite 存截图元数据、对话历史、审计日志
打包	未提及	electron-builder（Windows 优先）	目标用户 95% 为 Windows 企业环境
更新机制	无	electron-updater（支持内网离线包模式）	企业内网无法访问公网更新服务，须支持本地文件更新
四、重定义 MVP（Phase 1）
MVP 的唯一核心假设
"在物理隔离的内网环境下，开发者能否通过采集卡→截图→VLM 这一链路，在 30 秒内还原一个完整函数/类的代码，准确率超过 85%？"

所有两周内的工作，只服务于验证这一个假设。不验证假设的功能，一律砍掉。

验证成功的三个量化标准
准确率：代码还原字符级准确率 > 85%（用已知代码文件做 ground truth 对比）

端到端延迟：热键触发 → 代码可见 < 30 秒（P90）

可用率：无需人工拼接/修正，可直接使用的案例 > 60%

两周功能清单
✅ Phase 1 保留（核心验证链路，缺一不可）：

采集卡设备枚举 + 实时预览（无需缩放/亮度调节）

全局热键截图（Ctrl+Shift+S），截图存内存 Ring Buffer，最多 8 帧

帧差分去重（像素 diff，阈值 5%），自动丢弃静态重复帧

一键"提取代码"（Ctrl+Shift+E）：8 帧 → Claude API → 结构化 JSON 输出

代码只读展示区（<pre> 标签 + 一键复制按钮，不需要 Monaco Editor）

系统托盘常驻 + 右下角 Toast 通知（截图入队确认）

❌ Phase 1 砍掉（Phase 2/3 再做）：

Obsidian 归档（Phase 2）

多轮 AI 对话（Phase 2）

本地 VLM / 合规路由（Phase 2）

Mini 悬浮窗问答界面（Phase 2）

脱敏层（Phase 2；MVP 阶段以文档警告替代："请勿截取含敏感信息的画面"）

拖拽排序、删除、手动截图队列管理（Phase 2）

项目管理、历史会话、代码高亮（Phase 3）

两周开发排期
text
Week 1 ── 基础链路打通
  Day 1-2  Electron Forge + Vite 初始化，getUserMedia 枚举采集卡，实时预览
  Day 3-4  electron globalShortcut 全局热键，Ring Buffer 内存管理，系统托盘
  Day 5    Sharp 图像压缩（1080p→768px），帧差分去重算法

Week 2 ── AI 集成 + 发布
  Day 6-7  Claude 3.5 Sonnet API 集成，结构化多帧 Prompt，JSON 解析
  Day 8    代码展示 UI + 一键复制，Toast 通知
  Day 9    错误处理（API 超时/无采集卡信号），electron-builder Windows 打包
  Day 10   内部测试，灰度发布给 3 名目标外包开发者，收集验证数据
MVP 结束时的核心交付物
不是一个"完整产品"，而是一份假设验证报告：基于 3 名真实外包开发者、共计 ≥ 50 次代码提取的量化数据，证明或证伪上述三个成功标准。这份数据，才是 Phase 2 立项和融资的真正底牌。